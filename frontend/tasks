features:
    ? tests

    init window
    edit words

    change word result during game
    set number of words to guessing

    statistic
    косметика
    прокрутка для word_zone
    окно с ошибкой в случае падения запросов